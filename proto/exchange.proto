syntax = "proto3";

package exchange;
// option go_package = "baseapp";

service DataService {
    rpc SendData(DataRequest) returns (DataResponse);
}

message DataRequest {
    string msg = 1;
}

message DataResponse {
    string status = 1;
}